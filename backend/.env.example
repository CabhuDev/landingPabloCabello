# =====================================================
#         Configuración de Variables de Entorno
# =====================================================
#
# Instrucciones:
# 1. Renombra este archivo a ".env".
# 2. Rellena los valores con tus credenciales reales.
# 3. IMPORTANTE: El archivo .env NUNCA debe ser compartido ni subido a un repositorio (Git ya lo ignora gracias al .gitignore).

# --- Configuración del Servidor de Correo (SMTP) ---
# Estos son los datos para conectarse a tu proveedor de correo.
# Ejemplo para Gmail:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587

EMAIL_HOST=smtp.example.com
EMAIL_PORT=587

# --- Credenciales de Autenticación ---
# Tu usuario y contraseña de correo.
# NOTA DE SEGURIDAD: Si usas Gmail, es muy recomendable generar una "Contraseña de Aplicación" 
# en la configuración de seguridad de tu cuenta de Google y usarla aquí en lugar de tu contraseña principal.

EMAIL_USERNAME=tu-usuario@example.com
EMAIL_PASSWORD=tu-contraseña-de-aplicacion

# --- Direcciones de Envío ---
# La dirección desde la que se enviarán los correos y a la que llegarán.

EMAIL_FROM=remitente@example.com
EMAIL_TO=destinatario@example.com
